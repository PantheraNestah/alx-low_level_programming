#include <stdlib.h>
#include "main.h"
/**
 * _abs - returns the absolute value of an integer argument
 * @i: integer argument whose absolute value is to be returned
 * Return: the absolute value of integer argument i
 */
int _abs(int i)
{
	return (abs(i));
}
