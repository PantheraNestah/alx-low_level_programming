#include "main.h"
/**
 * add - adds two integers passed as arguments
 * @i: first integer argument passed to add function
 * @j: second integer argument passed to add function
 * Return: value of sum of i and j
 */
int add(int i, int j)
{
	return (i + j);
}
