#include "main.h"
/**
 * print_diagonal - prints a diagonal line by printing \ character
 * @n: number of times the character \ is printed
 * _putchar - prints character
 */
void print_diagonal(int n)
{
	int i = 1;

	if (n <= 0)
		_putchar('\n');
	else
	{
		while (i <= n)
		{
			int j
		}
	}
}
