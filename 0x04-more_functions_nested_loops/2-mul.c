#include "main.h"
/**
 * mul - multiplies two integer arguments and returns product
 * @a - first integer argument passed to mul to be multiplied by b
 * @b - second integer argument passed to mul to be multiplied by a
 * Return: product of a and b
 */
int mul(int a, int b)
{
	int ans = a * b;

	return (ans);
}
