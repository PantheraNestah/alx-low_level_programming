#include "main.h"
#include <string.h>
/**
 * _strlen - calculates and returns length of string pointed to by s
 * @s: pointer to string whose length is calculated
 * Return: length of the string pointed to by s
 */
int _strlen(char *s)
{
	return (strlen(s));
}
